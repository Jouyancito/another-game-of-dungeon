[gd_resource type="Resource" script_class="SummonResource" load_steps=2 format=3]

[ext_resource type="Script" path="res://shared/skills/summon_resource.gd" id="1_sumres"]

[resource]
script = ExtResource("1_sumres")
summon_id = &"skeleton_base"
display_name = "Esqueleto Básico"
hp_formula = "50 + INT*4 + level*5"
dmg_formula = "(8 + 0) * (1 + INT*0.02) * (1 + level*0.03) * 0.8"
duration_s = 60.0
max_active = 2
ai_behavior = &"guard_caster"
summon_scene = null
